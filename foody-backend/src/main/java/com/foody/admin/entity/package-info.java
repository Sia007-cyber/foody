/** Intentionally empty: the admin module has no entities of its own in Phase 1 —
 * it operates on Business/User/Order/Reservation entities owned by their
 * respective modules, accessed only through those modules' services. */
package com.foody.admin.entity;
